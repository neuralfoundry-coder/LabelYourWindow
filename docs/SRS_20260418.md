# Software Requirements Specification (SRS)

**Project:** LabelYourWindow
**Version:** 1.2.0
**Date:** 2026-04-18
**Platform:** macOS 15.0+ (Sequoia), Apple Silicon (arm64)

---

## 1. Overview

LabelYourWindow is a macOS menu bar application that displays translucent label overlays on windows to help users identify what each window is for in a multi-window workflow. Labels are auto-detected from window titles, customizable via rules or manual editing, and rendered as floating transparent overlays.

---

## 2. Functional Requirements

### FR-01: Window Focus Detection

| ID | Requirement | Status |
|----|------------|--------|
| FR-01-01 | Detect app activation via `NSWorkspace.didActivateApplicationNotification` | Implemented |
| FR-01-02 | Detect window focus changes via `AXObserver` (`kAXFocusedWindowChangedNotification`) | Implemented |
| FR-01-03 | Detect window move/resize via `kAXWindowMovedNotification`, `kAXWindowResizedNotification` | Implemented |
| FR-01-04 | Detect window title changes via `kAXTitleChangedNotification` | Implemented |
| FR-01-05 | Fallback to `CGWindowListCopyWindowInfo` when AX API fails (-25212, -25204) | Implemented |
| FR-01-06 | Retry AX queries with progressive delays (300ms, 500ms, 1000ms) | Implemented |
| FR-01-07 | Extract window position, size, title, app name, bundle ID from AX/CG APIs | Implemented |
| FR-01-08 | Enumerate all visible windows via `CGWindowListCopyWindowInfo` (layer=0 filter) | Implemented |
| FR-01-09 | Cache window titles from AX events for unfocused window label resolution | Implemented |

### FR-02: Label Resolution

| ID | Requirement | Status |
|----|------------|--------|
| FR-02-01 | Priority order: Window-level > Manual (app-level) > Rule > Auto-detect | Implemented |
| FR-02-02 | App-level manual labels keyed by `bundleID:windowTitle` (persisted) | Implemented |
| FR-02-03 | Window-level labels keyed by `pid:windowNumber` (session-scoped, not persisted) | Implemented |
| FR-02-04 | Rule-based matching by app name, window title, or bundle ID | Implemented |
| FR-02-05 | Regex pattern support in rules | Implemented |
| FR-02-06 | Label template variables: `{title}`, `{app}`, `{bundle}` | Implemented |
| FR-02-07 | Rule priority ordering (higher value = higher priority) | Implemented |
| FR-02-08 | Auto-detect via `WindowTitleParser` with app-specific patterns | Implemented |
| FR-02-09 | Label cached per window (pid:windowNumber); resolved once, reused on re-focus | Implemented |
| FR-02-10 | Cache invalidated on manual reset or window close | Implemented |

### FR-03: Window Title Parsing (Auto-detect)

| ID | Requirement | Status |
|----|------------|--------|
| FR-03-01 | Browsers: strip suffix (" - Google Chrome", " - Safari", etc.) | Implemented |
| FR-03-02 | Editors: extract `filename (project)` from `file — project — editor` | Implemented |
| FR-03-03 | Terminal: extract path from `user@host: ~/path` | Implemented |
| FR-03-04 | Finder: use raw title (folder name) | Implemented |
| FR-03-05 | Generic: strip app name suffix, take first part before separator | Implemented |

### FR-04: Overlay Display

| ID | Requirement | Status |
|----|------------|--------|
| FR-04-01 | Render label as `NSPanel` (borderless, non-activating, floating) | Implemented |
| FR-04-02 | Click-through by default (`ignoresMouseEvents = true`) | Implemented |
| FR-04-03 | Visible on all Spaces and in fullscreen | Implemented |
| FR-04-04 | Never steal focus from other apps (`canBecomeKey = false` unless in edit mode) | Implemented |
| FR-04-05 | Default position relative to target window frame; recalculated on each display unless user-dragged | Implemented |
| FR-04-06 | Clamp position to visible screen area (4pt margin) | Implemented |
| FR-04-07 | Hide previous overlay when switching to different window (single-window mode only) | Implemented |
| FR-04-08 | Overlay tracks window position on move/resize in multi-window mode | Implemented |

### FR-05: Display Modes

| ID | Requirement | Status |
|----|------------|--------|
| FR-05-01 | **Fade Away**: fade in (0.25s), stay for `fadeDuration`, fade out (`fadeAnimationDuration`) | Implemented |
| FR-05-02 | **Pinned**: fade in and remain visible until window changes | Implemented |
| FR-05-03 | Cancel pending fade when new focus event arrives | Implemented |
| FR-05-04 | Toggle mode from menu bar popover | Implemented |
| FR-05-05 | **Multi-window mode**: all visible windows show pinned labels simultaneously | Implemented |

### FR-06: Label Interaction

| ID | Requirement | Status |
|----|------------|--------|
| FR-06-01 | Hover over label to enable drag mode (visual cursor feedback) | Implemented |
| FR-06-02 | Drag label to reposition; persist absolute position via UserDefaults | Implemented |
| FR-06-03 | Edit label text from menu bar popover | Implemented |
| FR-06-04 | Reset manual label back to auto-detected | Implemented |
| FR-06-05 | Double-click overlay to enter inline edit mode | Implemented |
| FR-06-06 | Inline edit: text field with Save (Enter/checkmark) and Cancel (Esc/×) | Implemented |
| FR-06-07 | Inline edit saves as window-level label (session-scoped) | Implemented |

### FR-07: Appearance

| ID | Requirement | Status |
|----|------------|--------|
| FR-07-01 | Glass effect: `NSVisualEffectView` with `.hudWindow` material, behind-window blending | Implemented |
| FR-07-02 | Solid background: configurable opacity (10-100%) | Implemented |
| FR-07-03 | White text (0.9 opacity), rounded font design | Implemented |
| FR-07-04 | Configurable: font size (10-32pt), weight (9 levels), corner radius (0-20pt) | Implemented |
| FR-07-05 | Subtle white border (0.2 opacity, 0.5pt); edit mode border (0.4 opacity, 1pt) | Implemented |

### FR-08: Settings UI

| ID | Requirement | Status |
|----|------------|--------|
| FR-08-01 | **General tab**: enable/disable, multi-window mode toggle, mode, fade duration, position, inset, launch at login | Implemented |
| FR-08-02 | **Appearance tab**: font size/weight, glass toggle, opacity, corner radius, live preview | Implemented |
| FR-08-03 | **Rules tab**: list view, add/edit/delete, enable/disable toggle, priority, regex toggle | Implemented |
| FR-08-04 | Rule editor sheet (400x380) with pattern, template, match type, priority | Implemented |
| FR-08-05 | Accessibility status indicator with grant button | Implemented |

### FR-09: Menu Bar

| ID | Requirement | Status |
|----|------------|--------|
| FR-09-01 | `MenuBarExtra` with `tag.fill` system icon, window-style popover | Implemented |
| FR-09-02 | Show current window app name and label | Implemented |
| FR-09-03 | Label source badge (Window/Custom/Rule/Auto) | Implemented |
| FR-09-04 | Inline label editing with save/cancel/reset | Implemented |
| FR-09-05 | Display mode toggle button | Implemented |
| FR-09-06 | Settings link, quit button | Implemented |

### FR-10: App Lifecycle

| ID | Requirement | Status |
|----|------------|--------|
| FR-10-01 | `LSUIElement = true` (no Dock icon) | Implemented |
| FR-10-02 | Request Accessibility permission on launch with explanation string | Implemented |
| FR-10-03 | Poll for permission grant every 2s if not yet trusted | Implemented |
| FR-10-04 | Auto-start observation when permission granted (no restart needed) | Implemented |
| FR-10-05 | Launch at login via `SMAppService` | Implemented |
| FR-10-06 | Clean shutdown: cancel tasks, stop observers, hide overlays | Implemented |
| FR-10-07 | Multi-window poll loop (500ms interval) for stale overlay cleanup and position tracking | Implemented |

---

## 3. Non-Functional Requirements

| ID | Requirement | Status |
|----|------------|--------|
| NFR-01 | Event-driven architecture for focused window; polling (500ms) for all-windows enumeration | Implemented |
| NFR-02 | Minimal CPU usage (0% idle in single-window mode) | Verified |
| NFR-03 | Memory footprint < 100MB | Verified (~80MB) |
| NFR-04 | No sandbox (required for Accessibility API) | Implemented |
| NFR-05 | Stable code signing for persistent AX permission | Implemented |
| NFR-06 | Settings persistence via UserDefaults | Implemented |
| NFR-07 | Multi-monitor support (screen detection, position clamping) | Implemented |
| NFR-08 | No flickering on window move/resize (identity-only equality check) | Implemented |
| NFR-09 | User-dragged overlay positions persisted to UserDefaults across app restarts | Implemented |
| NFR-10 | Without Screen Recording permission, unfocused window titles resolved from AX cache | Implemented |

---

## 4. Configuration Defaults

| Setting | Default | Range |
|---------|---------|-------|
| Display Mode | Fade Away | Fade Away / Pinned |
| Multi-window Mode | Off | On / Off |
| Fade Duration | 2.0s | 0.5 - 10.0s |
| Fade Animation | 0.5s | 0.1 - 2.0s |
| Label Position | Top Right | 6 positions |
| Label Inset | 12pt | 0 - 50pt |
| Font Size | 14pt | 10 - 32pt |
| Font Weight | Medium (5) | 1 (UltraLight) - 9 (Black) |
| Glass Effect | On | On / Off |
| Background Opacity | 60% | 10 - 100% |
| Corner Radius | 8pt | 0 - 20pt |
| Enabled | Yes | Yes / No |
| Launch at Login | No | Yes / No |
| Allow Dragging | Yes | Yes / No |

---

## 5. Architecture

```
WindowObserver (AXObserver + NSWorkspace + CGWindowList polling)
    ↓ currentWindow changes (@Observable)        ↓ allVisibleWindows() called every 500ms
LabelManager (window-level > manual > rule > auto priority)
    ↓ label resolved
OverlayManager (NSPanel + animation)
    ↓ overlay shown/hidden/edited
OverlayWindow (NSPanel) + LabelOverlayView (SwiftUI via NSHostingView)
    ↑ double-click → inline edit mode (canBecomeKey = true temporarily)
```

**Key Files:**
- `LabelYourWindow/Core/WindowObserver.swift` - Window focus detection + all-windows enumeration
- `LabelYourWindow/Core/LabelManager.swift` - Label resolution engine (window-level + app-level)
- `LabelYourWindow/Core/OverlayManager.swift` - Overlay lifecycle, animation, edit mode
- `LabelYourWindow/Core/SettingsManager.swift` - Settings persistence
- `LabelYourWindow/Views/Overlay/OverlayWindow.swift` - Floating panel with double-click + edit mode key handling
- `LabelYourWindow/Views/Overlay/LabelOverlayView.swift` - Label UI + glass effect + inline edit UI
- `LabelYourWindow/Utilities/WindowTitleParser.swift` - Title parsing engine

---

## 6. Revision History

| Date | Version | Changes |
|------|---------|---------|
| 2026-04-16 | 1.0.0 | Initial SRS based on implemented features |
| 2026-04-16 | 1.1.0 | Label caching per window, absolute position persistence, hover-to-drag, flickering fix |
| 2026-04-18 | 1.2.0 | Multi-window labeling mode, overlay inline edit (double-click), window-level labels, position tracking fix |
